import Image from "next/image"

interface ProjectCardProps {
    image: string
}

export const ProjectCards = ({ image }: ProjectCardProps) => {
    return (
        <div className="">
            
        </div>
    )
}